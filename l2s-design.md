# 将 L2S 思路融入 HLS 的设计方案

## 1. 目标和边界

目标是在不替换 HLS 现有 COPT 初始化、Greedy、GA、Tabu 和全局 makespan 评估的前提下，引入 L2S 的两个有效机制：在**完整调度解**上构造关键路径跨机插入邻域；从多个候选中选择值得精确评价和执行的动作，同时利用历史状态减少重复搜索。学习模型用于候选排序或提议，不负责决定可行性和最终 makespan。

当前实施状态：`GuidedSearch.cpp` 已完成第一版完整跨机动作、规则分层 Top-48、最多 6 条持久轨迹、近期状态去重和动作频率惩罚，并作为可关闭的第四方法接入控制器。尚未实现 head/tail 安全预筛、跨 Greedy/Tabu 的公共候选引擎、近邻向量记忆和学习模型。规则分数或未来模型分数都不能代替 HLS 精确 makespan。

参考实现：`/home/luopw24/L2S-FJSP/src/envs/environment.py` 的 `_gen_moves`、`findLandR`，`test.py` 的 `evaluate_actions`，`src/envs/memory.py` 以及 `src/models/ppo_model.py`。L2S 每步从**已经排完的调度**中移动一条关键路径上的工序，换到另一台兼容机器的可行位置；采样多个候选、精确评价、执行本轮最优动作，并保存全程最优。这不是从空调度开始逐道排工序。

HLS 现状与接入点：

- `Schedule.h` 持有机器工序序列、图和 makespan；`Schedule.cpp` 已有 `GetKeyProcess()` 和 `GetProcessSlack()`。
- `DataProc.cpp` 中 `ScheduleItemsToGraph()` + `CalculateTotalTime()` 是目前各搜索方法共用的精确目标函数；后者对有环图抛异常。
- `GreedySearch.cpp` 和 `TabuSearch.cpp` 已有关键工序跨机迁移，但分别受候选排序、枚举预算和截止时间影响，尚无 L2S 风格的统一候选生成/选择接口。
- `main.cpp` 维护 `input_schedule`（工作解）、`output_schedule`（全局最好）和精英库；`AdaptiveSearch.cpp` 依据实际全局收益/耗时选择搜索方法。

最新可用的十次 HLS 实验 `run_logs/20260915_164533/result.csv` 中，Mk10 平均 224.6（221～228）；L2S 仓库保存的 `result.csv` 中 Mk10 平均 215.8（214～217）。两组实验的起点、硬件、时限和候选评价量未对齐，**不能**据此保证融合后能达到 215.8；这里只用于确定 Mk10 值得重点评估。

## 2. 推荐的整体流程

```text
COPT 或 HLS 多起点生成完整可行解
  -> HLS Greedy / GA / Tabu 寻找新解
  -> 停滞时从当前工作解或多样化精英解开启 L2S 邻域调用
  -> 枚举关键路径工序的跨机插入候选并去重
  -> 规则或学习模型挑选有限数量的候选
  -> 使用 HLS 精确图评价选中的完整调度
  -> 更新工作解、全局最好解和历史记忆
  -> 将全局改进及耗时反馈给自适应控制器
```

每次 L2S 调用可以执行多轮移动，但必须受统一的 `SearchDeadline` 控制。维护两个不同状态：`working_schedule` 允许有限次数等值或受控变差移动以走出局部最优；`best_schedule` 只在 makespan 严格降低时更新。方法返回最佳可行解，工作轨迹按实例/运行保存供下一次调用继续；不得把变差解覆盖外层全局最好。

## 3. 第一阶段：确定性候选邻域和精确评价

这一阶段不引入学习依赖，先验证邻域本身是否有效。建议新增独立的 L2S 搜索模块，避免把动作生成逻辑塞入 `main.cpp`，并尽可能复用 Greedy/Tabu 的图构造与评估路径。拟定的接口（名称可在实施时按实际代码调整）：

```cpp
struct InsertAction {
    int job_id;
    int process_id;
    int target_machine_id;
    int insert_before_job_id;      // -1 表示插在目标机器末尾
    int insert_before_process_id;  // -1 表示插在目标机器末尾
};

struct EvaluatedMove {
    InsertAction action;
    Schedule schedule;
    int makespan;
};
```

候选生成规则：

1. 对当前完整可行解计算最早/最迟时间、机器顺序和关键路径。初版可使用 `GetKeyProcess()` 的零松弛工序集合，但它可能同时包含多条关键路径，需分散抽样；停滞后再按小松弛阈值扩展到近关键工序。
2. 对每道工序枚举 `Job_process.process_item` 中除当前机器外的兼容机器，保留实际机器加工时间；仅在新机器中插入，现有同机 Greedy/Tabu 邻域保持独立。
3. 先保证正确性：枚举目标机器的全部插入槽位（包括空机器和末尾），使用现有图评价过滤形成环的动作。不能直接照搬 L2S 的头/尾时间二分公式：两项目的图边权定义、工序 ID 和虚拟节点不同。
4. 记录每个候选的完整动作三元组 `(operation, target_machine, insertion_slot)`；避免复制 L2S 的二维 `(operation, target_operation)` 动作键，因为当目标是虚拟终点等情况时，不同目标机器可能对应相同二维键，造成动作覆盖。
5. 工序总数/唯一性和机器兼容性先做轻量检查；对候选做一次精确拓扑评价，同时检测环并计算 makespan，不额外做一遍完整拓扑排序。候选使用机器工序序列签名去重；异常或超时返回无效候选，不改变当前解。

第一版使用规则评分（关键块位置、目标机器负载、加工时间、head/tail、松弛时间）排序，并从不同工序/机器均衡挑选候选；对前 `K` 个精确评价，按最小 makespan 选本轮动作。`K`、单次调用轮数和单次最大秒数作为配置项，初始建议 `K=16/32/64` 做消融，**不能**只增大 `K` 而忽略 Mk08～Mk10 的完整图重建成本。

正确性测试至少覆盖：跨机操作保持工件先后约束、源/目标机器的首/中/末插入、空目标机器、没有其他兼容机器的工序、同工件前后工序冲突导致的环、不同机器的相同“末尾”位置不混淆。对每个被接受候选重新调用 HLS 精确评估，保证记录 makespan 与实际一致且全局最好单调不增；某状态没有跨机动作时应正常返回原解或交还其他方法，而非空列表崩溃。

## 4. 第二阶段：状态记忆与多起点搜索

先实现轻量记忆，不直接移植 L2S 的 NumPy kNN 数据结构。按 `(instance 数据签名, 本次随机种子)` 隔离状态；新 instance、独立运行以及不同规模必须清空。一个状态签名可以包含按稳定工序 ID 排列的机器分配及每台机器相邻工序边；完整调度签名用于避免回访，动作签名包含目标机器和槽位。

- 短期：最近访问解的哈希集合、最近逆移动属性；近似 Tabu 限制重复移动，但允许刷新全局最优的 aspiration。
- 长期：保留少量高质量且结构不同的精英解，按机器分配差异和机器顺序边差异评估多样性；容量设上限，不将高 makespan 历史解误记为全局最佳。
- 停滞时：从另一个精英解出发做跨机插入，或在受控等值/变差步数内继续当前轨迹；设最大变差幅度、步数和回退阈值。可按 L2S 思路用相似状态的既往动作**惩罚重复**，但记忆中的动作必须先重新验证可行性，不能直接重放。

接入 `main.cpp` 的原则：L2S 作为第四种 `SearchMethod`，与 Greedy/GA/Tabu 共享总截止时间和调用统计；`AdaptiveSearchController` 记录“刷新全局最好值的下降量 / 实际耗时”和候选评估数。对旧精英的局部改善只给少量多样性信用，不得以此压过全局收益。深停滞允许 L2S 和 GA 轮流提供不同工作起点，不硬编码固定方法配比；保持原三方法可通过开关单独测试。

## 5. 第三阶段：学习排序器（独立于 C++ 求解器）

仅当第一、二阶段在相同时间预算下产生稳定收益时，再增加离线模型。推荐学习**候选动作排序**而不是立即重写整个 HLS 为端到端 PPO：HLS 负责生成可行动作和真实标签，模型只对有限候选打分；模型不可用时自动退化为规则排序。训练与线上推理要使用完全相同的工序 ID、目标机器和槽位编码。

训练数据获取：在不同规模的合成 FJSP 实例及训练集上运行当前 HLS，从中采样不同起点（含 COPT 初始、GA 新解、Tabu 停滞解）；保存实例标识、调度结构、可行动作、精确候选 makespan、访问记录和时间成本。监督排序可用同一状态下候选精确 makespan 的排序或相对于工作解的下降量；保留等值/变差路径作为后续策略训练数据，避免训练集只包含严格改进动作。划分按**实例**而非单条动作，Brandimarte Mk01～Mk10 作为最终留出测试时不可参与训练或调参。

模型选择与部署：

- 起步使用简单排序器或小型模型并与规则排序对照；当图结构编码带来真实增益时，再考虑 L2S 的工序/机器图特征和 PPO。机器编码必须进入动作特征，不复用其仅针对 `(移动工序, 目标工序)` 的二维得分查表。
- 现有 L2S checkpoint 有 10x5、15x10、20x5、20x10 等规模，没有直接针对 Mk10（20 工件、15 机器）的同规模 checkpoint；直接拿 10x5 模型接入 HLS 不保证尺寸、特征归一化或效果。复用预训练模型需先离线验证跨规模泛化和完整动作映射；不能把 Python/PyTorch 或 GPU 变成 HLS 默认运行的硬依赖。
- 若模型确实有效，再定义显式导出格式及可选推理后端（如可验证的 ONNX 导出）；明确设备、线程、模型版本、缺失模型处理、推理耗时上限和每进程内状态隔离。五路/十路实验需计入 CPU/GPU 资源竞争和推理耗时。
- PPO 可作为后续实验：训练时将全局 best 改进作为主奖励，对循环状态增加轻量惩罚；动作先采样多个候选再精确选优。需要将被选中动作的概率与执行动作严格对应，不能只用被采样但未执行动作的收益更新策略。

## 6. 性能、验证与交付顺序

HLS 当前对每个候选在 `DataProc.cpp` 重建稠密邻接矩阵并做全图拓扑评价，候选数增大后容易让模型挑选收益被评价耗时抵消。先测量候选生成、去重、图验证、精确评价和模型推理各自耗时；必要时将公共候选评估改为稀疏图或安全的增量评价，并保留旧版全图计算作为一致性对照。优化评价器与引入新邻域应分开做 A/B，避免无法归因。

建议依次交付并逐项通过门槛：

1. **基线和校验（部分完成）**：第四方法支持 `HLS_ENABLE_GUIDED_SEARCH=0` A/B，动作检查源调度签名并复用完整图评价；仍需补充独立属性测试和候选计数日志。
2. **L2S 规则邻域（基础完成）**：已实现跨机全槽位、规则分层 Top-48 和规模/失败门控；下一步比较 head/tail 安全预筛及不同 `K`，先测召回率再上线。
3. **记忆和调度（基础完成）**：已有 6 条轨迹、128 个近期状态和动作频率衰减；下一步提取共享候选引擎并记录可行候选比例、重复访问率和边际收益。
4. **学习排序**：规则排序对比监督排序，最后才评估 PPO、预训练权重迁移和嵌入 C++ 推理；每步都保留无模型回退路径。
5. **最终验收**：同一数据、同一总时限（包括 COPT、候选评价和推理）、同一并发资源、同一 seed 集合下比较 HLS 基线与各阶段。报告每个 Mk01～Mk10 的 best/mean/median/worst、耗时和方差，重点看 Mk04/Mk05/Mk09 已有收益是否保留，以及 Mk10 的平均值和最差值是否实质改善；不以单次最好成绩判断成功。

实验输出继续写独立时间目录，至少附参数、seed、代码/模型版本、每次方法调用和候选评价数。`RUNS=10`、`PARALLEL_JOBS=5` 可复用现有 `run_brandimarte_experiments.sh`；新增方法的时间必须包含在原 3600 秒总预算内。
